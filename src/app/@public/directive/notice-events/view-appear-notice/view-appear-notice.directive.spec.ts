import { ViewAppearNoticeDirective } from './view-appear-notice.directive';

describe('ViewAppearNoticeDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewAppearNoticeDirective();
    expect(directive).toBeTruthy();
  });
});
