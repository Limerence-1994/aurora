import { OnceClickDirective } from './once-click.directive';

describe('OnceClickDirective', () => {
  it('should create an instance', () => {
    const directive = new OnceClickDirective();
    expect(directive).toBeTruthy();
  });
});
